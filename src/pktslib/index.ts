import { sendAppMessage } from "./appMessage";
import { fetchString, fetchJSON, fetchBinary } from "./fetch";

export { sendAppMessage, fetchString, fetchJSON, fetchBinary };

export default {
  sendAppMessage,
  fetchString,
  fetchJSON,
  fetchBinary,
};
