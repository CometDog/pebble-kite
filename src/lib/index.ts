import {
  createLogger,
  initDebugMode,
  setDebugMode,
  isDebugMode,
} from "./logger";

export { createLogger, initDebugMode, setDebugMode, isDebugMode };

export default {
  createLogger,
  initDebugMode,
  setDebugMode,
  isDebugMode,
};
